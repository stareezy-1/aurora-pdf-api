name: Deploy

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: Deploy to VPS
        env:
          ENV_B64: ${{ secrets.ENV_B64 }}
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          envs: ENV_B64
          script: |
            set -e

            mkdir -p ~/aurora-pdf-api
            echo "$ENV_B64" | base64 -d > ~/aurora-pdf-api/.env
            chmod 600 ~/aurora-pdf-api/.env

            cd ~/aurora-pdf-api
            sudo systemctl stop aurora-pdf-api 2>/dev/null || true
            sudo systemctl disable aurora-pdf-api 2>/dev/null || true

            if ! command -v docker &>/dev/null; then
              curl -fsSL https://get.docker.com | sh
              sudo usermod -aG docker "$USER"
            fi

            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f
